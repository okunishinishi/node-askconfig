/**
 * Test case for question.
 * Runs with mocha.
 */
"use strict";

const question = require('../lib/question.js'),
    assert = require('assert');

describe('question', () => {

    before((done) => {
        done();
    });

    after((done) => {
        done();
    });


    it('Question', (done) => {
        done();
    });
});

